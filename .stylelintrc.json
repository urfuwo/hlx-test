{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "custom-property-pattern": [
      "^(.*)$",
      {
        "message": "Expected custom property does not match pattern"
      }
    ]
  }
}
